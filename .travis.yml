language: php

cache:
  directories:
    - $HOME/.composer/cache

php:
  - '7.3'
  - '7.4'

before_script:
  - flags="-o"
  - composer install $flags

script: composer run-script test

after_success:
  - bash <(curl -s https://codecov.io/bash)
